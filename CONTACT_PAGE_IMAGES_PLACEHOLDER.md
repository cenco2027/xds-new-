# Contact Page 图片占位说明

## 📸 图片资源状态

由于Figma MCP工具的路径限制，图片需要手动下载。目前CSS中已配置了正确的图片路径，但图片文件尚未下载。

### 当前状态

**已配置路径但文件缺失**:
- `assets/images/contact/hero-bg-desktop.png`
- `assets/images/contact/hero-bg-tablet.png`
- `assets/images/contact/hero-bg-mobile.png`

### 解决方案

#### 选项1: 手动下载图片（推荐）
参考 `CONTACT_PAGE_IMAGES_DOWNLOAD_STATUS.md` 中的详细步骤：
1. 打开Figma设计文件
2. 选择对应的节点
3. 导出为PNG格式（2x scale）
4. 保存到对应路径

#### 选项2: 使用占位图片
可以先使用纯色背景或渐变作为占位，图片下载后替换即可。

#### 选项3: 使用现有类似图片
可以临时使用其他页面的背景图片作为占位（如 `assets/images/hero/hero-bg-desktop.png`）

---

## 🔄 图片路径配置

CSS文件中已配置以下路径：

```css
/* Desktop */
background-image: url('../../assets/images/contact/hero-bg-desktop.png');

/* Tablet */
background-image: url('../../assets/images/contact/hero-bg-tablet.png');

/* Mobile */
background-image: url('../../assets/images/contact/hero-bg-mobile.png');
```

图片下载后，路径会自动生效。

---

## 📋 图标资源

表单中的下拉箭头和提交按钮图标当前使用内联SVG。如需替换为实际图标文件，参考：
- `CONTACT_PAGE_IMAGES_DOWNLOAD_STATUS.md` - 图标下载清单

---

**注意**: 即使没有图片文件，页面结构和样式仍然可以正常工作，只是背景会显示为纯色或渐变。



